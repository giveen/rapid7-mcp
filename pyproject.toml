[project]
name = "rapid7-mcp"
version = "0.1.0"
description = "MCP server for Rapid7 InsightVM — exposes vulnerability management tools to LLM clients"
authors = [{ name = "Tim Hollingsworth" }]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.136.3",
    "fastapi-mcp>=0.3",
    "httpx>=0.27",
    "pydantic-settings>=2.14",
    "uvicorn[standard]>=0.49",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=1.4",
    "respx>=0.23",
    "ruff>=0.15",
    "mypy>=2.1",
    "pytest-cov>=6.0",
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501", "B008"]  # B008: FastAPI's Depends() in defaults is intentional

[tool.mypy]
python_version = "3.11"
strict = false
ignore_missing_imports = true
